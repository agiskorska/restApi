{
  "name": "new-wave-fest-app",
  "version": "1.0.1",
  "main": "server.js",
  "license": "MIT",
  "scripts": {
    "start": "node server.js",
    "build": "cd client && yarn install && yarn build"
  },
  "dependencies": {
    "express": "4.17.1"
  },
  "devDependencies": {
    "cors": "^2.8.5",
    "node": "^16.6.1",
    "nodemon": "^2.0.12"
  },
  "engines": {
    "node": "15.3.0"
  }
}
