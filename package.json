{
  "name": "new-wave-fest-app",
  "version": "1.0.1",
  "main": "server.js",
  "license": "MIT",
  "scripts": {
    "start": "nodemon server.js",
    "build": "cd client && yarn install && yarn build"
  },
  "dependencies": {
    "express": "4.17.1"
  },
  "devDependencies": {
    "cors": "^2.8.5"
  }
}
